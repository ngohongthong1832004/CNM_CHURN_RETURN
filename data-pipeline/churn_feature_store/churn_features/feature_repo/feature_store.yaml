project: churn_mlops
provider: local

registry: data/registry.db

# ONLINE STORE (Redis)
online_store:
  type: redis
  connection_string: ${FEAST_REDIS_CONNECTION_STRING}

# OFFLINE STORE
offline_store:
  type: file

entity_key_serialization_version: 3
