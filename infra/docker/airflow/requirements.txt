requests
confluent-kafka>=2.3

# MLflow tracking & registry
# mlflow>=2.16 supports numpy 2.x (2.15 pinned numpy<2)
mlflow>=2.16.0
boto3>=1.34.59
s3fs>=2024.1.0

# Feature store
# feast 0.58.0 requires numpy>=2.0.0
feast==0.58.0
redis==5.0.4

# Data processing — all pinned to numpy 2.x compatible versions
numpy>=2.0.0
pandas>=2.2.2
pyarrow>=16.0.0
scikit-learn>=1.5.0

# Lakehouse
# 0.8.0+ drops numpy<2.0.0 requirement
pyiceberg[pyarrow,s3fs]>=0.8.0
trino==0.329.0

# Boosting models
xgboost>=2.1.1
lightgbm>=4.5.0
catboost>=1.2.7

# Utilities
loguru==0.7.2
pyyaml==6.0.1
